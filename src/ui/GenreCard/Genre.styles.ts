import styled from "styled-components";
import Typography from "@mui/material/Typography";

export const GenreName = styled(Typography)`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translateX(-50%);
  color: #ffffff;
  font-weight: 700;
`;
